<h4>Enter your post info:</h4>

<%= form_for @post do |f| %>
  <%= f.label :body %>
  <%= f.text_field :body, maxlength: 170 %>

  <%= f.hidden_field :user_id, value: current_user.id %>

  <%= f.submit "Create!" %>
<% end %>
